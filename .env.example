# Copy to .env and set a strong secret
JWT_SECRET=change-me-to-a-long-random-secret
JWT_EXPIRES_IN=7d
ALLOW_REGISTRATION=true
DEFAULT_USERNAME=Ark
# DEFAULT_PASSWORD=
REDDIT_CACHE_BUDGET_MB=1024
# Comma-separated allowed origins (your app domain)
CORS_ORIGINS=https://signal.protogen.tools
